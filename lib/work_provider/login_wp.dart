import 'package:flutter/material.dart';

class Login_Wp extends StatelessWidget {
  const Login_Wp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This Login Screen is for Work Provider")),
    );
  }
}
